{
  "name": "ha-scrypted-camera-card",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Home Assistant Lovelace card streaming Scrypted cameras over WebRTC, with two-way audio",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@scrypted/client": "^1.3.26",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
